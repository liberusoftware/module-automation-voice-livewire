name: Compatibility

on:
  push:
    tags: ['[0-9]+.[0-9]+.[0-9]+']
  workflow_dispatch:

jobs:
  compatibility:
    uses: liberusoftware/.github/.github/workflows/package-compatibility.yml@75d1786646b095ba49360f735f27e0b2fa932b8d
